# AgentCore Gateway Target submodule

Creates one native Gateway Target. Runtime and MCP targets support AgentCore Identity providers, JWT passthrough, Gateway IAM, or caller IAM credentials without changing the root module's existing target resources.
