# AgentCore Managed Harness submodule

Creates one Managed Harness with explicit model, execution, network, authorization, memory, and budget settings. No tools are enabled by default, and wildcard tool access is rejected.

Use the focused example at [`examples/managed-harness`](../../examples/managed-harness).
