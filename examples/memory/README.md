# Memory

Creates one AgentCore Memory and one semantic strategy. No Runtime or other
AgentCore capability is enabled.

When copying this example, replace `source = "../.."` with the registry source
and pin the module version.
