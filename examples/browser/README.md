# Browser

Creates one AgentCore Browser and one reusable Browser Profile. No Runtime or
other AgentCore capability is enabled.

When copying this example, replace `source = "../.."` with the registry source
and pin the module version.
