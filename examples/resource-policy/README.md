# AgentCore Resource Policy

Attaches a resource policy to an existing AgentCore Gateway without creating
or requiring a Policy Engine. The same `resource_policies` map can protect
Runtimes and other AgentCore resources supported by AWS.
