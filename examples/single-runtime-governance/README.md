# Single Runtime with governance

This example keeps one Runtime per module invocation while opting into the
Policy Engine, Gateway, Memory, Browser, and online evaluation resources used
by that Runtime. Deploy additional Runtime lanes with separate module calls so
their IAM, lifecycle, and state remain easy to review.
