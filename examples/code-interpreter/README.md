# Code Interpreter

Creates one AgentCore Code Interpreter using the no-network `SANDBOX` default.
No Runtime or other AgentCore capability is enabled.

When copying this example, replace `source = "../.."` with the registry source
and pin the module version.
