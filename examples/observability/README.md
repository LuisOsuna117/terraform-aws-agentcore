# Observability

Creates one CloudWatch log group for AgentCore telemetry with the module's
365-day retention default. No Runtime or other AgentCore capability is enabled.

When copying this example, replace `source = "../.."` with the registry source
and pin the module version.
